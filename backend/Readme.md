Steps to run:

1. Set up .env with DB connection values
2. Run docker: docker-compose up -d
3. Install server dependencies: yarn
4. Migrate DB: yarn migrate
5. Seed DB: yarn seed
6. Run DEV server: yarn dev

To run a test: yarn test

Please make sure that all tests pass whenever you change something on the backend!
